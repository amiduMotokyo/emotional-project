# A：特征、对齐与视频证据

负责M2问题1成果核查、M3时间映射与证据定位，并承担论文统稿。

- `src/`：已有问题1代码整理、特征核查和时间映射实现。
- `configs/`：提取工具、模型、对齐粒度等参数。
- `scripts/`：提取、汇总、证据导出的运行入口。
- `outputs/`：100条自生成特征、处理日志、汇总及视频证据产物。
- [个人文档](../docs/A/README.md)：需求、创新调研、实验说明和论文。

问题1代码尚待交接。接入后补充准确的环境、运行命令和输出路径。向B交付证据定位接口时说明时间单位、精度和样本对应关系。论文合并稿存放于 `docs/A/paper/`。
