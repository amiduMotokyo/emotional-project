# 问题二：输入一致性修订实验

状态：已训练、完成验证与附件2留出测试评估、生成附件3结果；归档于 C/M5、M7。

### 附件2测试集补评

`label.xlsx` 实际含 `train` 3395条、`valid` 728条和 `test` 727条。初版实验只将 `train` 和 `valid` 纳入管线，遗漏了 `test`；本次已补做测试评估。测试样本编号、类别和强度标签已按视频/片段编号与 `aligned_50.pkl` 逐条核对。模型和超参数仍按验证集锁定，测试集没有参与训练或模型选择。

测试集同时报告原始有效输入，以及按验证协议固定构造的63种连续缺失条件；这不是对每条测试样本随机遮挡一次。另将三种候选结构的三个验证集选定检查点种子作同口径事后比较，仅用于报告泛化结果，不据此改选最终模型。附件3仍为另一组无标签推理样本。

## 目的与关键发现

原流程把完整 `text_bert` 先编码为 MiniLM 向量，再通过池化掩码删除位置。附件3的缺失词元却是在编码器输入中表现为 `[UNK]`。上下文编码会把被删词的信息带入其他位置，因此原实验没有严格复现转写输入损坏。

复核也发现，随包动态量化 ONNX 编码器对同一词元使用单条和批量输入时，输出会有可测差异。旧缓存和附件3单条推理由此使用了不同向量。修订实验统一采用随包 int8 ONNX 编码器，并对训练、验证、受损文本视图和附件3按单条输入。重建缓存后抽样复算，与同批次单条ONNX输出逐项一致。

旧缓存和旧结果保留作历史记录，不与本实验的指标直接合并比较。旧实验所示方法是“编码后特征遮挡”；文献简化变体的结果也没有在本次统一接口下重跑。

## 数据和掩码

使用 `aligned_50.pkl`：训练3395条、验证728条、测试727条；附件3的30个对齐样本只作最后推理。文本向量为MiniLM输出384维，音频74维，视觉35维。音视频的均值和标准差只在训练集、原始有效位置拟合，标准化后无效点仍设为零。

对模态 (m) 的位置 (t)，观测状态来自其基础有效掩码和新增人工遮挡掩码。填充、原始无效值和人工缺失分别处理。缺失区间在50步原坐标上直接取连续起止位置；不根据有效点总数重编号。训练时每条样本独立决定是否缺失、缺失模态组合、比例与区间。

训练集每条样本以0.8概率进入受损视图，缺失比例从10%、20%、30%、50%中抽取，七种非空模态组合均可出现，位置随机。保存8组互相独立的词元级受损视图，训练周期中轮换。被遮文本在编码前替换成 `[UNK]`，保留attention mask；音频和视觉在相同区间由掩码屏蔽。附件3中实测 `[UNK]` 的attention保持为有效，这与修订后的词元替换规则一致。

## 模型与训练

同一训练协议下比较三种结构：干净输入门控基线、连续缺失增强门控模型、带位置卷积和双向GRU的时序模型。最终选择由验证集比较，不预设更复杂结构一定更好。

基础分类和回归损失为：

\[
\mathcal L=\operatorname{CE}_{w}(c,p)+0.8\,\operatorname{SmoothL1}(y,\hat y),
\qquad
w_c=\sqrt{N/(3N_c)},\qquad \hat y=3\tanh(z).
\]

三种候选均使用AdamW，学习率 (8\times10^{-4})、权重衰减0.01、batch size 64、最多35轮、早停7轮、梯度裁剪1.0。随机种子为20260924、20260925、20260926。标准化和所有参数仅使用训练集拟合。

每轮在干净验证集及文本、音频、视觉各自30%中段缺失场景上评估。选择分数为四个条件平均后的 `Macro-F1 - 0.15×MAE + 0.05×Pearson`。三种子平均选择分数为：

| 候选 | 平均选择分数 ± 标准差 | 63种条件下平均 Macro-F1 | 63种条件下平均 MAE（未做符号一致性解码） |
|---|---:|---:|---:|
| 干净输入门控基线 | 0.5100 ± 0.0107 | 0.5642 | 0.6552 |
| 连续缺失增强门控 | 0.5182 ± 0.0080 | 0.5706 | 0.6354 |
| 时序门控候选 | 0.5162 ± 0.0064 | 0.5652 | 0.6472 |

网格包含7种非空模态组合、3种缺失率、3种区间位置，共63种条件，每种728条验证样本。三类完整网格的MAE按原始回归头输出统计。最终采用连续缺失增强门控结构，种子20260924；时序候选未带来更高的验证选择分数，差异不大，不据此断言某结构普遍更优。

## 最终验证与缺失规律

最终模型原始双头输出及交付解码结果如下。交付时由分类结果约束强度符号，并将Neutral强度设为0；因此分类指标不变，强度指标按实际交付规则重算。

| 指标 | 原始双头 | 实际交付解码 |
|---|---:|---:|
| Accuracy | 0.6168 | 0.6168 |
| Macro-F1 | 0.5942 | 0.5942 |
| MAE | 0.5971 | 0.5901 |
| Pearson | 0.6488 | 0.6490 |

交付解码下，负向/中性/正向F1分别为0.6504、0.4482、0.6841；混淆矩阵（行为真实类别，列为预测类别）为 `[[133,31,42],[30,80,74],[40,62,236]]`。中性仍是主要分类误差来源。

最终选中模型的63种条件均按交付解码重新评估，平均Accuracy为0.5972、Macro-F1为0.5761、MAE为0.6076、Pearson为0.6071；最差条件Macro-F1为0.5306。按缺失模态对所有比例和位置取平均：

| 缺失模态 | Macro-F1 | MAE |
|---|---:|---:|
| 文本 | 0.5638 | 0.6210 |
| 音频 | 0.5949 | 0.5896 |
| 视觉 | 0.5925 | 0.5896 |

按缺失率对全部模态组合和位置取平均，10%、30%、50%的Macro-F1分别为0.5884、0.5769、0.5630；MAE分别为0.5959、0.6085、0.6184。文本缺失影响最大，缺失增加时整体Macro-F1下降、MAE上升。音频缺失的影响较小，视觉介于两者之间。三模态同步或错位缺失的全部数值见 `served_grid.csv`。

交付强度与极性一致：Neutral固定输出0；Positive不输出负值；Negative不输出正值。此解码在验证集上的MAE和Pearson优于原始回归头，因此用于附件3。

## 附件2留出测试结果

按验证集选定的连续缺失增强门控模型（种子20260924）在干净测试输入上的交付解码结果为：Accuracy 0.6272、Macro-F1 0.5865、MAE 0.6554、Pearson 0.6193。负向/中性/正向F1分别为0.6479、0.3937、0.7181；混淆矩阵为 `[[138,36,33],[34,62,62],[47,59,256]]`。在63种缺失条件下，平均Accuracy为0.6111、Macro-F1为0.5716、MAE为0.6824、Pearson为0.5840；最差Macro-F1为0.5341。

同一测试集上的三种子候选对比（原始回归头，均值±标准差）如下。`clean_gate` 不使用新增人工缺失增强；`robust_gate` 使用缺失增强；时序候选也使用缺失增强。

| 候选 | 干净 Macro-F1 | 干净 MAE | 63格平均 Macro-F1 | 63格平均 MAE |
|---|---:|---:|---:|---:|
| 干净输入门控基线 | 0.5855 ± 0.0102 | 0.6968 ± 0.0134 | 0.5693 ± 0.0049 | 0.7207 ± 0.0158 |
| 连续缺失增强门控 | 0.5914 ± 0.0037 | 0.6775 ± 0.0163 | 0.5703 ± 0.0016 | 0.7010 ± 0.0127 |
| 时序门控候选 | 0.5896 ± 0.0074 | 0.6870 ± 0.0111 | 0.5659 ± 0.0063 | 0.7104 ± 0.0091 |

测试集显示缺失增强的优势主要体现在强度误差与跨种子稳定性，Macro-F1相对干净基线的增益较小；因此不把结果描述为显著提升。

在最终模型的测试集63格中，按单模态、位置与缺失比例分别平均的性能规律为：

| 因素 | 条件 | Macro-F1 | MAE | Pearson |
|---|---|---:|---:|---:|
| 单模态缺失 | 文本 | 0.5603 | 0.7018 | 0.5587 |
|  | 音频 | 0.5845 | 0.6567 | 0.6191 |
|  | 视觉 | 0.5874 | 0.6550 | 0.6204 |
| 区间位置 | 开头 | 0.5785 | 0.6817 | 0.5888 |
|  | 中间 | 0.5695 | 0.6845 | 0.5810 |
|  | 结尾 | 0.5667 | 0.6810 | 0.5821 |
| 缺失比例 | 10% | 0.5815 | 0.6648 | 0.6088 |
|  | 30% | 0.5702 | 0.6809 | 0.5897 |
|  | 50% | 0.5629 | 0.7016 | 0.5535 |

测试集复现了验证集的主要趋势：文本缺失最不利，缺失区间越长，Macro-F1和Pearson越低、MAE越高；位置差异较小，强度MAE并非随位置单调变化。缺失率仍表示50步对齐序列有效跨度的比例，不是秒数。

## 附件2测试、附件3与复现文件

附件3的30条样本均有唯一编号，输出类别为负向6条、中性6条、正向18条；中性非零强度0条。附件3无标签，不报告准确率。预测和类别概率见 `attachment3_predictions.csv`。

可复现代码为 `B/scripts/reencode_q2_cache.py`、`C/src/missingness.py`、`C/src/q2_protocol.py`、`C/scripts/run_q2_corrected.py`、`C/scripts/analyze_q2_corrected.py` 和 `C/scripts/infer_q2_corrected.py`。运行入口、配置、环境和最终模型随 `question2_corrected.zip` 提供。训练、缓存、验证网格及日志位于服务器目录 `/mnt/disk/data/inainai/mosei_e_20260924/q2_corrected_20260924/`；大缓存不纳入 Git。

附件2测试评估入口为 `C/scripts/evaluate_q2_attachment2_test.py`。测试结果文件为 `attachment2_test_summary.json`、`attachment2_test_candidates.csv`、`attachment2_test_candidate_grid.csv`、`attachment2_test_predictions.csv` 和 `attachment2_test_selected_grid.csv`。测试运行也保存在服务器目录 `/mnt/disk/data/inainai/mosei_e_20260924/q2_corrected_20260924/`；大缓存不纳入 Git。

服务器环境为Python 3.12、PyTorch 2.7.0+cu126、NumPy 2.2.5、scikit-learn 1.6.1、ONNX Runtime 1.20.1；GPU为RTX 2080 Ti。编码器为冻结的 `sentence-transformers/all-MiniLM-L6-v2` 动态int8 ONNX。

## 限制

附件2没有逐词时间戳，本文缺失时长用50步有效序列跨度的比例表示，不折算成秒。附件3没有标签，只检验完整推理与文件覆盖，不用于选模型。局部缺失规律只适用于当前 MOSEI 划分和模拟范围；验证和测试集都显示中性类别较难识别。
